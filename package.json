{
  "name": "tcc-realtime-serving",
  "version": "1.0.0",
  "description": "Load test for realtime serving models using onnx and pytorch",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "k6": "^0.0.0"
  }
}
